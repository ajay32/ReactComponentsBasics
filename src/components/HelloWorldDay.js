import React from "react"; 


// if the export is not default you have to import with the same component name "HelloWorld"
// we can export multiple components from single file
export const HelloWorld = () => {
    return <h1>Hello India, Russia, America</h1>;
};

export const WhatWhat = () => {
    return <h1>WhatWhat</h1>;
};